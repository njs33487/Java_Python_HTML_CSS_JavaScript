package exercise01;

public class ExerciseOne {

	/*
	 * Create a method named "solution" that takes in two integer arguments,
	 * adds those arguments together, multiplies that value by 10, then
	 * return that value.
	 * 
	 * example:
	 * 		With an input of 10 and 20
	 * 		The method should return 300
	 */
	
}
