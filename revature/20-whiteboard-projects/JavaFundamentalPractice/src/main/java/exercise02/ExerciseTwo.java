package exercise02;

public class ExerciseTwo {
	
	/*
	 * Create a STATIC method named "solution" that takes
	 *  in three string arguments. It will three put those three arguments
	 *  into an array, in the exact same order, then return that array.
	 *  
	 *  Example:
	 *  		With input "big", "bad", and "wolf"
	 *  		The method should return an array containing
	 *  			["big", "bad", "wolf"]
	 */

}
