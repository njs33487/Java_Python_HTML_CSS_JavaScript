package exercise11;

public class ExerciseEleven {

	/*
	 * Create a method called "solution" that takes in a String as a
	 * argument. The method should return a boolean determining if that
	 * string is a palindrome or not. True is it IS a palindrome.
	 * (Google what a palindrome is if you need)
	 * 
	 * Example:
	 * 		Given input "mom", output should be true
	 * 		Given input "racecar", output should be true
	 * 		Given input "giraffe", output should be false
	 */
	
}
